# خطوات Deploy الكود الجديد على Render

## المشكلة الحالية ✅ تم تحديدها

- ✅ الـ category "Pellets" موجود في قاعدة البيانات على Render
- ✅ الـ category حالته Active
- ✅ التغييرات موجودة على GitHub (commit 78c607a)
- ❌ الكود الجديد لم يتم deploy على Render بعد
- ❌ لهذا لا يظهر في API endpoint

## الحل - خطوتان بسيطتان

### الخطوة 1️⃣: اذهب إلى Render Dashboard

1. افتح: https://dashboard.render.com/
2. ابحث عن service اسمه **"onebby-api"**
3. اضغط عليه للدخول

### الخطوة 2️⃣: قم بـ Manual Deploy

1. في أعلى الصفحة، ستجد زر أزرق اسمه **"Manual Deploy"**
2. اضغط عليه
3. اختر **"Deploy latest commit"**
4. انتظر 2-3 دقائق حتى يكتمل الـ deployment

ستلاحظ:
- Building... ⏳
- Deploying... ⏳
- Live ✅ (عندما ينتهي)

### الخطوة 3️⃣: اختبر API بعد Deploy

بعد ما يصبح Status = **Live** 🟢، جرب API مرة أخرى:

```http
GET https://onebby-api.onrender.com/api/v1/categories?lang=en
X-API-Key: your-api-key
```

يجب أن يظهر "Pellets" الآن! ✅

## ماذا حدث؟

| الخطوة | الحالة |
|--------|--------|
| 1. أضفت category "Pellets" في Dashboard | ✅ تم |
| 2. الـ category حُفظ في قاعدة البيانات | ✅ تم |
| 3. قمنا بإصلاح الكود وحفظه على GitHub | ✅ تم |
| 4. **Deploy الكود الجديد على Render** | ⏳ **يجب القيام به الآن** |
| 5. الـ category سيظهر في API | ⏳ بعد Deploy |

## لماذا لم يحدث Auto-Deploy؟

Render يقوم بـ auto-deploy عادة، لكن:
- قد يكون Auto-Deploy معطل
- أو يحتاج بضع دقائق ليبدأ
- أو توجد إعدادات خاصة

للتأكد من Auto-Deploy في المستقبل:
1. Render Dashboard → onebby-api → **Settings**
2. تحت "Build & Deploy"
3. تأكد من: **Auto-Deploy: Yes** ✅

## التحقق من نجاح Deploy

بعد Deploy، يمكنك التحقق:

```bash
# في Terminal
curl -H "X-API-Key: your-key" "https://onebby-api.onrender.com/api/v1/categories?lang=en"
```

أو استخدم الملف:
```bash
python test_render_api.py
```

## الخلاصة

🎯 **الحل بسيط**: اضغط "Manual Deploy" في Render Dashboard، وانتظر 2-3 دقائق!

بعدها سيعمل كل شيء بشكل صحيح ✨
