# التحديثات الجديدة - Categories API

## 📋 ملخص التحديثات

تم إعادة بناء نظام الفئات بالكامل مع إضافة endpoint جديد وتحسينات كبيرة على الهيكل والأداء.

---

## 🔑 1. إزالة API Key من GET Endpoints

### التغيير الأول: تسهيل الوصول للبيانات العامة

**قبل:**
```javascript
// كان يتطلب API Key في headers
fetch('/api/v1/products', {
  headers: { 'X-API-Key': 'your-key' }
})
```

**بعد:**
```javascript
// لم يعد يحتاج API Key للقراءة فقط
fetch('/api/v1/products')
```

### Endpoints المتأثرة:
- ✅ `GET /api/v1/products` - قائمة المنتجات
- ✅ `GET /api/v1/products/{id}` - منتج واحد
- ✅ `GET /api/v1/categories` - قائمة الفئات
- ✅ `GET /api/v1/categories/{id}` - فئة واحدة
- ✅ `GET /api/v1/categories/{id}/children` - أطفال الفئة
- ✅ `GET /api/v1/brands` - قائمة الماركات
- ✅ `GET /api/v1/tax-classes` - فئات الضرائب

### ملاحظة مهمة:
- ❌ API Key **لا يزال مطلوباً** لـ POST/PUT/DELETE
- ✅ API Key **غير مطلوب** لـ GET فقط

---

## 🌳 2. هيكل شجرة الفئات الجديد

### المواصفات:
- **إجمالي الفئات:** 134 فئة
- **المستويات:** 3 مستويات (Parent → Child → Grandson)
- **الفئات الرئيسية:** 8 فئات
- **الفئات الفرعية:** 84 فئة
- **الأحفاد:** 42 فئة

### الهيكل:
```
المستوى 1 (Parent)
  └─ المستوى 2 (Child)
      └─ المستوى 3 (Grandson)
```

---

## 📊 3. الفئات الرئيسية الثمانية

| ID | الاسم | عدد الأطفال | عدد الأحفاد |
|----|-------|-------------|-------------|
| 8151 | Grandi elettrodomestici | 8 | 3 |
| 8152 | Elettrodomestici incasso | 12 | 0 |
| 8153 | Audio video | 13 | 10 |
| 8154 | Clima | 6 | 4 |
| 8155 | Elettrodomestici cucina | 12 | 16 |
| 8156 | Cura della persona | 15 | 0 |
| 8157 | Informatica | 20 | 4 |
| 8158 | Telefonia | 5 | 0 |

---

## 🆕 4. Endpoint جديد: /subcategories

### الوصف:
يرجع **جميع الأحفاد** (المستوى الثالث) تحت فئة رئيسية معينة دفعة واحدة.

### الاستخدام:
```
GET /api/v1/categories/{category_id}/subcategories
```

### متى تستخدمه:
- ✅ عندما تريد جلب **كل الأحفاد** من فئة رئيسية
- ✅ لتقليل عدد الطلبات للـ API
- ✅ للفئات الرئيسية الـ 8 فقط

### Parameters:
- `category_id` (required): ID الفئة الرئيسية (8151-8158)
- `lang` (optional): كود اللغة (مثل: 'ar', 'en', 'it')

### أمثلة:

#### مثال 1: جلب أحفاد Elettrodomestici cucina
```bash
GET https://onebby-api.onrender.com/api/v1/categories/8155/subcategories
```

**Response:**
```json
{
  "data": [
    {
      "id": 8431,
      "name": "Passaverdure e passapomodoro",
      "slug": "passaverdure-e-passapomodoro",
      "image": null,
      "icon": null,
      "parent_id": 8385,
      "sort_order": 0,
      "is_active": true,
      "has_children": false
    },
    {
      "id": 8428,
      "name": "Cuociriso e pentole elettriche",
      "slug": "cuociriso-e-pentole-elettriche",
      "image": null,
      "icon": null,
      "parent_id": 8384,
      "sort_order": 0,
      "is_active": true,
      "has_children": false
    }
    // ... 14 أخرى (المجموع: 16)
  ],
  "meta": {
    "parent_id": 8155,
    "requested_lang": null,
    "resolved_lang": null,
    "total_subcategories": 16
  }
}
```

#### مثال 2: جلب أحفاد Audio video
```bash
GET https://onebby-api.onrender.com/api/v1/categories/8153/subcategories
```
**النتيجة:** 10 أحفاد (من Home Cinema, Televisori, إلخ)

---

## 📖 5. جميع Endpoints المتاحة

### 5.1 جلب كل الفئات
```
GET /api/v1/categories
```

**الوظيفة:** يرجع قائمة بجميع الفئات (134 فئة)

**Response:**
```json
{
  "data": [
    {
      "id": 8151,
      "name": "Grandi elettrodomestici",
      "slug": "grandi-elettrodomestici",
      "parent_id": null,
      "has_children": true,
      "is_active": true
    }
    // ... 133 فئة أخرى
  ]
}
```

---

### 5.2 جلب فئة واحدة
```
GET /api/v1/categories/{category_id}
```

**مثال:**
```bash
GET /api/v1/categories/8151
```

**Response:**
```json
{
  "data": {
    "id": 8151,
    "name": "Grandi elettrodomestici",
    "slug": "grandi-elettrodomestici",
    "image": null,
    "icon": null,
    "parent_id": null,
    "sort_order": 1,
    "is_active": true,
    "has_children": true
  }
}
```

---

### 5.3 جلب الأطفال المباشرين
```
GET /api/v1/categories/{category_id}/children
```

**الوظيفة:** يرجع الأطفال **المباشرين** فقط لأي فئة

**مثال 1 - فئة رئيسية:**
```bash
GET /api/v1/categories/8151/children
```
**النتيجة:** 8 أطفال (Lavatrici, Asciugatrici, إلخ)

**مثال 2 - فئة فرعية:**
```bash
GET /api/v1/categories/8287/children
```
**النتيجة:** 3 أحفاد (Lavatrici Carica Frontale, إلخ)

---

### 5.4 جلب جميع الأحفاد (الجديد!) ⭐
```
GET /api/v1/categories/{category_id}/subcategories
```

**الوظيفة:** يرجع **جميع الأحفاد** (المستوى الثالث) تحت فئة رئيسية

**مثال:**
```bash
GET /api/v1/categories/8155/subcategories
```
**النتيجة:** 16 حفيد من جميع الأطفال

---

## 🔄 6. الفرق بين /children و /subcategories

| Feature | /children | /subcategories |
|---------|-----------|----------------|
| **يعمل مع** | أي فئة (رئيسية أو فرعية) | فئات رئيسية فقط (8151-8158) |
| **يرجع** | الأطفال المباشرين | جميع الأحفاد (مستوى 3) |
| **مثال 8151** | 8 أطفال فقط | 3 أحفاد |
| **مثال 8287** | 3 أحفاد | فارغ [] |
| **الاستخدام** | بناء قوائم منسدلة متدرجة | جلب كل الأحفاد دفعة واحدة |

---

## 💻 7. أمثلة JavaScript للفرونت اند

### 7.1 جلب الفئات الرئيسية
```javascript
async function getMainCategories() {
  const response = await fetch(
    'https://onebby-api.onrender.com/api/v1/categories'
  );
  const data = await response.json();
  
  // تصفية الفئات الرئيسية فقط
  const mainCategories = data.data.filter(cat => cat.parent_id === null);
  
  return mainCategories;
}
```

---

### 7.2 بناء قائمة منسدلة (Dropdown)
```javascript
async function buildCategoryDropdown(parentId) {
  const response = await fetch(
    `https://onebby-api.onrender.com/api/v1/categories/${parentId}/children`
  );
  const data = await response.json();
  
  return data.data.map(cat => ({
    value: cat.id,
    label: cat.name,
    hasChildren: cat.has_children
  }));
}

// مثال الاستخدام
const children = await buildCategoryDropdown(8151);
// النتيجة: [{value: 8287, label: "Lavatrici", hasChildren: true}, ...]
```

---

### 7.3 جلب كل الأحفاد دفعة واحدة
```javascript
async function getAllGrandchildren(mainCategoryId) {
  const response = await fetch(
    `https://onebby-api.onrender.com/api/v1/categories/${mainCategoryId}/subcategories`
  );
  const data = await response.json();
  
  console.log(`إجمالي الأحفاد: ${data.meta.total_subcategories}`);
  
  return data.data;
}

// مثال الاستخدام
const grandchildren = await getAllGrandchildren(8155);
// النتيجة: 16 حفيد من Elettrodomestici cucina
```

---

### 7.4 بناء شجرة كاملة (Full Tree)
```javascript
async function buildFullCategoryTree() {
  // 1. جلب كل الفئات
  const response = await fetch(
    'https://onebby-api.onrender.com/api/v1/categories'
  );
  const allCategories = await response.json();
  
  // 2. تنظيم الشجرة
  const tree = allCategories.data
    .filter(cat => cat.parent_id === null) // الفئات الرئيسية فقط
    .map(parent => ({
      ...parent,
      children: allCategories.data
        .filter(cat => cat.parent_id === parent.id)
        .map(child => ({
          ...child,
          grandchildren: allCategories.data
            .filter(gc => gc.parent_id === child.id)
        }))
    }));
  
  return tree;
}
```

---

### 7.5 البحث في الشجرة
```javascript
async function searchCategory(searchTerm) {
  const response = await fetch(
    'https://onebby-api.onrender.com/api/v1/categories'
  );
  const data = await response.json();
  
  return data.data.filter(cat => 
    cat.name.toLowerCase().includes(searchTerm.toLowerCase())
  );
}

// مثال الاستخدام
const results = await searchCategory('lava');
// النتيجة: Lavatrici, Lavastoviglie, إلخ
```

---

## 📝 8. تفاصيل الفئات الرئيسية

### 8.1 Grandi elettrodomestici (8151)
**الأطفال (8):**
- 8287 - Lavatrici (3 أحفاد)
- 8288 - Asciugatrici
- 8289 - Lavasciuga
- 8290 - Aspirapolvere e accessori
- 8291 - Scope elettriche
- 8292 - Robot aspirapolvere
- 8293 - Lucidatrici e pulitori a vapore
- 8294 - Stiratrici e accessori

**الأحفاد (3):**
- تحت 8287: Lavatrici Carica Frontale, Lavatrici Carica dall'Alto, Accessori Lavatrici

---

### 8.2 Audio video (8153)
**الأطفال (13):**
- 8368 - Televisori
- 8454 - Home Cinema (8 أحفاد)
- 8369 - HI-FI Home
- 8370 - Macchine fotografiche
- والمزيد...

**الأحفاد (10):**
- تحت 8454: Home Theatre e Soundbar, Videoproiettori, Decoder, إلخ
- تحت 8368: Cuffie per TV, Accessori TV

---

### 8.3 Clima (8154)
**الأطفال (6):**
- 8380 - Condizionatori (2 أحفاد)
- 8382 - Riscaldamento (2 أحفاد)
- 8317 - Ventilatori
- 8381 - Trattamento aria
- 8383 - Accessori
- 8318 - Stufe elettriche

**الأحفاد (4):**
- تحت 8380: Condizionatori Fissi, Condizionatori Portatili
- تحت 8382: Stufe elettriche, Stufe a combustibile

---

### 8.4 Elettrodomestici cucina (8155)
**الأطفال (12):**
- 8384 - Cottura cibi (7 أحفاد)
- 8385 - Preparazione cibi (9 أحفاد)
- 8386 - Macchine da caffè
- والمزيد...

**الأحفاد (16):**
- تحت 8384: Forni microonde, Friggitrici, Tostapane, إلخ (7)
- تحت 8385: Robot da cucina, Frullatori, Spremiagrumi, إلخ (9)

---

### 8.5 Informatica (8157)
**الأطفال (20):**
- 8352 - Computer portatili (4 أحفاد)
- 8392 - Tablet e accessori
- 8393 - Personal computer
- والمزيد...

**الأحفاد (4):**
- تحت 8352: Notebook, Borse per notebook, Alimentatori, Accessori

---

## 🎯 9. حالات الاستخدام الشائعة

### Scenario 1: عرض قائمة الفئات الرئيسية
```javascript
GET /api/v1/categories
// تصفية parent_id === null
```

### Scenario 2: عرض قائمة منسدلة للأطفال
```javascript
GET /api/v1/categories/{parent_id}/children
```

### Scenario 3: عرض جميع المنتجات في فئة وأطفالها
```javascript
// 1. جلب الفئة الرئيسية
// 2. جلب الأطفال
// 3. جلب المنتجات لكل فئة
```

### Scenario 4: جلب كل الأحفاد دفعة واحدة
```javascript
GET /api/v1/categories/{main_category_id}/subcategories
```

---

## ⚠️ 10. ملاحظات مهمة

### 10.1 الأداء
- ✅ `/children` أسرع - يجلب مستوى واحد فقط
- ⏱️ `/subcategories` قد يكون أبطأ - يجمع من عدة مستويات
- 💡 استخدم `/subcategories` فقط عند الحاجة لكل الأحفاد

### 10.2 Caching
- يُنصح بعمل cache للفئات في Frontend
- الفئات لا تتغير بشكل متكرر
- Cache validity: 1-24 ساعة

### 10.3 Error Handling
```javascript
try {
  const response = await fetch('/api/v1/categories/8151/children');
  
  if (!response.ok) {
    throw new Error(`HTTP error! status: ${response.status}`);
  }
  
  const data = await response.json();
  return data;
  
} catch (error) {
  console.error('خطأ في جلب الفئات:', error);
  // عرض رسالة خطأ للمستخدم
}
```

---

## 📞 11. معلومات الاتصال

**Base URL:**
```
https://onebby-api.onrender.com/api/v1
```

**API Key (للعمليات POST/PUT/DELETE فقط):**
```
X-API-Key: X9$eP!7wQ@3nZ8^tF#uL2rC6*mH1yB0_dV4+KpS%aGfJ5$qWzR!N7sT#hU9&bE
```

**Documentation:**
```
https://onebby-api.onrender.com/docs
```

---

## ✅ 12. Checklist للمطور

- [ ] تحديث Fetch calls - إزالة API Key من GET requests
- [ ] تطبيق `/subcategories` endpoint الجديد
- [ ] اختبار الشجرة الكاملة (134 فئة)
- [ ] التحقق من الـ 3 مستويات تعمل بشكل صحيح
- [ ] تطبيق Error handling
- [ ] إضافة Loading states
- [ ] اختبار على جميع الفئات الرئيسية الـ 8
- [ ] عمل Cache للفئات
- [ ] اختبار الأداء

---

## 📅 تاريخ التحديثات

**التاريخ:** 13 يناير 2026

**التغييرات الرئيسية:**
1. ✅ إزالة API Key من GET endpoints
2. ✅ إعادة بناء شجرة الفئات (134 فئة)
3. ✅ إضافة endpoint جديد: `/subcategories`
4. ✅ إصلاح depth limit ليدعم 3 مستويات
5. ✅ تنظيم جميع الفئات حسب Excel file

**الحالة:** ✅ جاهز للإنتاج
